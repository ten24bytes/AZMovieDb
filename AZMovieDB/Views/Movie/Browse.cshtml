@model AZMovieDB.Models.Genre
@{
    ViewBag.Title = "Browse";
}
<h2>Browsing Genre: @Model.Name</h2>
<ul>
    @foreach (var movie in Model.Movies)
    {
        <li>
            @Html.ActionLink(movie.Title, "Details", new { id = movie.MovieId })
        </li>
    }
</ul>
